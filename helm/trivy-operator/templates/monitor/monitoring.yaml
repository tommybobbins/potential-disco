apiVersion: monitoring.googleapis.com/v1
kind: PodMonitoring
metadata:
  name: prom-{{ .Chart.Name }}
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .Chart.Name }}
  endpoints:
  - port: 8080
    interval: 300s
